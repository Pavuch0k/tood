# Tood - Текстовый редактор для Hyprland

Минималистичный безрамочный текстовый редактор, совместимый с Hyprland.

## Особенности

- 🎨 Безрамочное окно, идеально для Hyprland
- 📝 Простой холст для редактирования текста
- 📑 Вкладки для работы с несколькими файлами
- 📄 **Поддержка Markdown файлов**:
  - Автоматическое открытие `.md` файлов в режиме предпросмотра
  - Кнопка переключения между предпросмотром и редактированием
  - Автоматическое обновление предпросмотра при редактировании
- 🎯 Автосортировка строк по маркерам (`-`, `!`, `+`)
- 🎨 Цветовая подсветка строк:
  - `-` в конце строки → красный фон
  - `!` в конце строки → жёлтый фон
  - `+` в конце строки → зелёный фон
- 🔢 Нумерация строк с подсветкой текущей
- 💾 Автосохранение в реальном времени
- ⌨️ Горячие клавиши:
  - `Ctrl+N` - новый файл
  - `Ctrl+O` - открыть файл
  - `Ctrl+S` - сохранить
  - `Ctrl+Shift+S` - сохранить как
  - `Ctrl+Q` - закрыть текущую вкладку
  - `Ctrl++` / `Ctrl+-` - масштабирование текста

## Установка

1. Клонируйте репозиторий:
```bash
git clone git@github.com:Pavuch0k/tood.git
cd tood
```

2. Создайте виртуальное окружение и установите зависимости:
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

3. Запустите приложение:
```bash
python main.py
```

## Работа с Markdown файлами

Приложение автоматически определяет файлы с расширением `.md` и открывает их в режиме предпросмотра:

- **Режим предпросмотра**: Markdown отображается в скомпилированном виде (HTML)
- **Режим редактирования**: Доступен исходный Markdown текст для редактирования
- **Переключение**: Кнопка "Редактировать" / "Предпросмотр" в правом верхнем углу
- **Автообновление**: Предпросмотр автоматически обновляется при изменении текста

## Настройка Hyprland

Добавьте в `~/.config/hypr/hyprland.conf`:

```ini
bind = SUPER, P, exec, /home/nik/tood/run_hyprtext.sh
```

Для автоматического открытия `.txt` и `.md` файлов через приложение, установите desktop-файл:

```bash
./install_desktop.sh
```

## Структура проекта

```
tood/
├── app/
│   ├── __init__.py
│   ├── app.py              # Точка входа приложения
│   ├── config.py           # Управление конфигурацией
│   ├── assets/
│   │   └── save.svg        # Иконка сохранения
│   ├── core/
│   │   ├── __init__.py
│   │   └── file_manager.py # Управление файлами
│   └── ui/
│       ├── __init__.py
│       └── main_window.py  # Главное окно
├── main.py                 # Запуск приложения
├── requirements.txt        # Зависимости
├── run_hyprtext.sh         # Скрипт запуска для Hyprland
└── README.md
```

## Лицензия

MIT



